You are an expert in spectroscopic data processing and Machine Learning. Your task is to extract detailed information regarding the preprocessing pipeline used for SERS data.

### **Mandatory Extraction Rules:**
1. **No Guessing**: If a specific step is not mentioned, use `null` for booleans and `"not reported"` for strings.
2. **Evidence Quote**: You MUST provide the exact sentence from the paper describing the preprocessing steps or the order of operations.
3. **Specific Methods**: Identify the specific algorithm name (e.g., "airPLS", "polyfit", "Savitzky-Golay") rather than general terms like "smoothing".

### **Data Structure Requirements:**

- **paper_id**: Any internal ID or DOI if no ID is found.
- **raw_or_processed_input**: Whether the model takes raw data or pre-processed data as input.
- **cosmic_ray_removal**: Boolean or string describing the method.
- **baseline_correction_method**: The specific algorithm used (e.g., "airPLS", "AsLS", "Rubberband", "not reported").
- **smoothing_method**: The algorithm used (e.g., "Savitzky-Golay", "Gaussian", "Moving Average").
- **normalization_method**: The technique used (e.g., "Vector normalization", "SNV", "Min-Max", "Standardization").
- **outlier_detection_method**: Any method used to remove bad spectra (e.g., "PCA-based", "3-sigma", "None").
- **dimensionality_reduction**: List of methods (e.g., ["PCA", "t-SNE", "UMAP"]).
- **feature_selection**: List of methods (e.g., ["Genetic Algorithm", "VIP scores", "RFE"]).
- **preprocessing_order**: List of steps in the order they were performed (e.g., ["Smoothing", "Baseline Correction", "Normalization"]).
- **software**: List of tools used (e.g., "LabSpec", "Python/scikit-learn", "MATLAB").
- **evidence_quote**: The exact text describing the preprocessing workflow.
- **doi**: The DOI string.

### **Target JSON Format:**
{
  "paper_id": "not reported",
  "raw_or_processed_input": "not reported",
  "cosmic_ray_removal": null,
  "baseline_correction_method": "not reported",
  "smoothing_method": "not reported",
  "normalization_method": "not reported",
  "outlier_detection_method": "not reported",
  "dimensionality_reduction": [],
  "feature_selection": [],
  "preprocessing_order": [],
  "software": [],
  "evidence_quote": "not reported",
  "doi": "not reported"
}

### **Paper Content:**
{content}
